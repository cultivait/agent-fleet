{
  "mcpServers": {
    "agent-fleet": {
      "command": "node",
      "args": ["plugin/dist/mcp-server.mjs"],
      "env": {
        "AGENT_FLEET_JOIN_TOKEN": "your-secret-value-here",
        "AGENT_FLEET_HUB_URL": "http://localhost:9559"
      }
    }
  }
}
