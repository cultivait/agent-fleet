node_modules/
dist/
!plugin/dist/
.env
.claude/settings.local.json
.claude/worktrees/
.cursor/mcp.json
docs/
*.db
*.db-wal
*.db-shm

# install.sh generated artifacts (local, contain secrets / runtime state — never commit)
.mcp.json
/data/
/logs/
.hub.pid

# one-time walkie-talkie->agent-fleet migration tool — a fresh clone-and-go user never
# runs it, so keep it out of the package (move to scripts/fleet/legacy/ if a record is wanted)
scripts/fleet/cutover.sh
